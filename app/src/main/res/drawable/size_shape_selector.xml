<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:drawable="@drawable/rectangle_selected_bg_text" android:state_selected="true" />
    <!-- Pressed state -->
    <item android:drawable="@drawable/rectangle_bg_text" android:state_pressed="true" />
    <!-- Default state -->
    <item android:drawable="@drawable/rectangle_bg_text" />
</selector>
